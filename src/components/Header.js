import React from 'react';
import '../App.css';

const Header = (props) => {

    const style = {
        height: "70px",
        backgroundColor : "cyan"
    }

    return (
        <div className='Header'>
            <h2>Header</h2>
        </div>
    );
}

export default Header;